#include<stdio.h>
#include<math.h>

float ac (float r){
    float a;
    a = 3.14 * (r * r);
    return a;
}
int main(void){
    float a, b;
        printf("Informe o valor do raio: ");
        scanf("%f", &b);
        a = ac(b);
        printf("O resultado e %.2f", a);

    return 0;
}
